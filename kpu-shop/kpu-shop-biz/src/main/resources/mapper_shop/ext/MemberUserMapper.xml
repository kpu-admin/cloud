<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.lmx.kpu.shop.mapper.user.MemberUserMapper">
<!--
    代码生成器 by 2025-08-21 02:42:27
    自定义sql建议在base文件夹同级新建ext文件夹，并新建同名且同namespace的xml进行编写。方便修改字段时，重新生成此文件。
-->
    <resultMap id="ResultVOResultMap" type="cn.lmx.kpu.shop.entity.user.MemberUser"
               extends="BaseResultMap">

    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Member_User_Column_List">
        id, username, `password`, salt, real_name, birthday,
        card_id, nick_name, avatar, mobile, add_ip,
        now_money, integral, `level`, password_error_last_time, password_error_num, state, user_type,
        login_type, sex, country, is_deleted, created_time,
        updated_time, created_by, updated_by
    </sql>

    <update id="incrPasswordErrorNumById">
        update shop_user
        set password_error_num       = password_error_num + 1,
            password_error_last_time = #{now, jdbcType=TIMESTAMP}
        where id = #{id, jdbcType=BIGINT}
    </update>
    <update id="resetPassErrorNum">
        update shop_user
        set password_error_num       = 0,
            password_error_last_time = null,
            last_login_time          = #{now, jdbcType=TIMESTAMP}
        where id = #{id, jdbcType=BIGINT}
    </update>

</mapper>
