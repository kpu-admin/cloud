spring:
  cloud:
    gateway:
      routes:
        # 从这里开始复制
        - id: sop-admin
          uri: lb://kpu-sop-admin-server
          predicates:
            - Path=/sop-admin/**
          filters:
            - StripPrefix=1

## 将配置文件的内容复制到：kpu-gateway-server.yml
## 然后删除本文件！！！
